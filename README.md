Contains the function `quote_html` which can be used to build reply emails,
forwarded emails etc. The tricky part is keeping styling for the quoted email
but resetting it for the added content.

Install for development: `pip install -e ".[dev]"`
